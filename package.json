{
  "name": "@pokt-foundation/node-nanny",
  "description": "Blockchain monitoring and automation.",
  "repository": "https://github.com/pokt-foundation/portal",
  "author": "JonathonJulian <john@pokt.network>",
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "build:watch": "tsc -b -w .",
    "start:all": "pnpm i && pnpm build && pm2 start node-nanny.config.js",
    "start:agent": "pnpm i && pnpm build && pm2 start node-nanny.config.js --only agent",
    "start:api": "pnpm i && pnpm build && pm2 start node-nanny.config.js --only graphql-api",
    "start:event": "pnpm i && pnpm build && pm2 start node-nanny.config.js --only event-consumer",
    "start:monitor": "pnpm i && pnpm build && pm2 start node-nanny.config.js --only monitor",
    "start:ui": "pnpm i && pnpm build && pm2 start node-nanny.config.js --only ui",
    "stop:all": "pm2 stop node-nanny.config.js",
    "stop:agent": "pm2 stop node-nanny.config.js --only agent",
    "stop:api": "pm2 stop node-nanny.config.js --only graphql-api",
    "stop:event": "pm2 stop node-nanny.config.js --only event-consumer",
    "stop:monitor": "pm2 stop node-nanny.config.js --only monitor",
    "stop:ui": "pm2 stop node-nanny.config.js --only ui",
    "restart:all": "pm2 restart node-nanny.config.js",
    "kill": "pm2 kill"
  },
  "devDependencies": {
    "prettier": "2.5.1",
    "turbo": "^1.1.3",
    "typescript": "4.5.5"
  },
  "packageManager": "pnpm@6.22.2"
}
