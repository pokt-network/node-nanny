{
  "name": "@pokt-foundation/node-nanny",
  "description": "Blockchain monitoring and automation.",
  "repository": "https://github.com/pokt-foundation/portal",
  "author": "JonathonJulian <john@pokt.network>",
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "build:clean": "turbo run build:clean",
    "build:fresh": "turbo run build:clean && turbo run build",
    "start:all": "export MONITOR_TEST=1 && pnpm i && pnpm build && pm2 start process.yml",
    "start:fresh": "pm2 kill && pnpm build:fresh && pnpm start:all",
    "start:api": "pnpm i && pnpm build && pm2 start process.yml --only api",
    "start:event": "pnpm i && pnpm build && pm2 start process.yml --only event-consumer",
    "start:monitor": "pnpm i && pnpm build && pm2 start process.yml --only monitor",
    "start:ui": "pnpm i && pnpm build && pm2 start process.yml --only ui",
    "stop:all": "pm2 stop process.yml",
    "stop:api": "pm2 stop process.yml --only graphql-api",
    "stop:event": "pm2 stop process.yml --only event-consumer",
    "stop:monitor": "pm2 stop process.yml --only monitor",
    "stop:ui": "pm2 stop process.yml --only ui",
    "restart:all": "pm2 restart process.yml",
    "kill": "pm2 kill",
    "monitor": "pm2 monit",
    "mongo": "mongosh local --authenticationDatabase admin -u root -p rootpassword"
  },
  "devDependencies": {
    "prettier": "2.5.1",
    "turbo": "^1.1.3",
    "typescript": "4.5.5"
  },
  "packageManager": "pnpm@6.32.3"
}
