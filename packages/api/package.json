{
  "name": "api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start:graphql": "pnpm build && ts-node -r tsconfig-paths/register ./src/gql-server.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b .",
    "build:watch": "tsc -b -w ."
  },
  "dependencies": {
    "@pokt-foundation/node-monitoring-core": "workspace:^0.1.0",
    "apollo-server": "^3.5.0",
    "dotenv": "^10.0.0",
    "graphql": "^16.0.1"
  },
  "devDependencies": {
    "ts-node": "^10.1.0",
    "tsconfig-paths": "^3.12.0"
  }
}
