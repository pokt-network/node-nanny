{
  "name": "@pokt-foundation/node-nanny-core",
  "description": "Shared package used by all backend packages",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b .",
    "postbuild": "cp package.json dist",
    "build:clean": "tsc --build --clean && rm -rf ./dist",
    "build:watch": "tsc -b -w .",
    "test": "pnpm build && jest --runInBand"
  },
  "dependencies": {
    "@pagerduty/pdjs": "^2.2.4",
    "aws-sdk": "^2.950.0",
    "axios": "^0.21.2",
    "axios-retry": "^3.2.4",
    "discord.js": "^13.6.0",
    "mongoose": "^6.3.2",
    "mongoose-paginate-v2": "^1.6.3",
    "winston": "^3.6.0",
    "winston-mongodb": "^5.0.7"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "tag": "latest",
    "access": "public"
  }
}
